<%= form_for(@contract_document) do |f| %>
  <% if @contract_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract_document.errors.count, "error") %> prohibited this contract_document from being saved:</h2>

      <ul>
      <% @contract_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contract_id %><br>
    <%= f.text_field :contract_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
