<%= form_for(@applying_company) do |f| %>
  <% if @applying_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applying_company.errors.count, "error") %> prohibited this applying_company from being saved:</h2>

      <ul>
      <% @applying_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
