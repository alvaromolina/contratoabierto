<%= form_for(@contract_form) do |f| %>
  <% if @contract_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract_form.errors.count, "error") %> prohibited this contract_form from being saved:</h2>

      <ul>
      <% @contract_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contract_id %><br>
    <%= f.text_field :contract_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
