<%= form_for(@motive) do |f| %>
  <% if @motive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motive.errors.count, "error") %> prohibited this motive from being saved:</h2>

      <ul>
      <% @motive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
